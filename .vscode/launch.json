{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "cds watch",
      "name": "swap Debug Session",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}